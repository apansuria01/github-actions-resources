name: first-workflow
on: workflow-dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run:  echo "Hello!"
      - name: Print Goodbye
        run: echo "DOne- bye!"

